# 莎岡選品店 — 系統規格書

這份文件描述系統**做什麼**、以及**為什麼這樣做**。
不談框架與檔案結構（那些在 [README.md](README.md)），談的是規則、金額怎麼算、
狀態怎麼流轉、哪些行為是刻意決定的。

適合用來：交接給接手的人、跟營運/客服對規則、決定下一步要補什麼。

---

## 1. 系統概觀

一個繁體中文的單一商店電商網站，賣韓國睡衣與家居選品。
消費者可以不註冊就完成購買；金流、物流、發票全部透過**綠界科技 ECPay** 處理。

### 角色

| 角色 | 能做什麼 | 怎麼認定 |
|---|---|---|
| 訪客 | 瀏覽、加購物車、結帳付款、用訂單編號查訂單 | 無 |
| 會員（CUSTOMER） | 訪客能做的全部 + 訂單歷史、地址簿、會員載具發票、撰寫評論 | 登入 |
| 管理員（ADMIN） | 全部後台作業 | `User.role = ADMIN` |

管理員只能由資料庫直接指派（`prisma/seed.ts` 建立第一個）。**後台沒有「把某人升為管理員」的介面**，
這是刻意的：權限提升不該是一個點兩下就能做的事。

---

## 2. 商業參數（營運槓桿）

這些是可以在不改程式的情況下調整的營運參數，全部在 `.env`：

| 參數 | 預設 | 意義 |
|---|---|---|
| `SHIPPING_FEE_CVS` | 60 | 超商取貨運費（元） |
| `SHIPPING_FEE_HOME` | 120 | 宅配運費（元） |
| `FREE_SHIPPING_THRESHOLD` | 1500 | 免運門檻（元） |
| `STOCK_RESERVATION_MINUTES` | 30 | 未付款訂單保留庫存的時間 |
| `ECPAY_INVOICE_AUTO_ISSUE` | true | 付款成功後是否自動開立發票 |
| `SHOP_NAME` / `SHOP_TAX_ID` | 莎岡選品店 / 93124857 | 顯示於頁面、發票、信件 |
| `SMS_PROVIDER` | console | `console`=不真的發送（開發用）、`mitake`=三竹 |

**寫死在程式裡、目前不能用環境變數調的**（要改需改程式）：

| 項目 | 值 | 位置 |
|---|---|---|
| 商品列表每頁筆數 | 24 | `src/lib/catalog/queries.ts` |
| 後台「低庫存」警示門檻 | ≤ 3 | `src/app/admin/page.tsx` |
| 前台「僅剩 N 件」提示門檻 | ≤ 5 | `src/components/product/add-to-cart.tsx` |
| 登入有效期 | 30 天 | `src/lib/auth/config.ts` |
| 單一商品最大購買量 | 99 | `src/lib/cart/actions.ts` |
| 圖片單張大小上限 | 6 MB | `src/lib/uploads.ts` |
| 圖片最長邊上限 | 1600 px | `src/lib/uploads.ts` |
| 一次可上傳張數 | 10 | `src/lib/uploads.ts` |

> 前台的 5 件與後台的 3 件是**兩件不同的事**：前者是給消費者的稀缺感提示，
> 後者是給營運的補貨警示。不需要一致，但要知道它們是分開的。

---

## 3. 功能規格

### 3.1 商品目錄

**資料層級**：品牌 → 商品 → 規格（變體）。商品可同時屬於多個分類。

**分類與品牌**都由後台維護（`/admin/taxonomy`），共用一套規則：

- slug 由名稱自動產生，**建立後不可修改**（對外連結與 SEO）
- 排序數字越小越前面，決定導覽列與首頁品牌區塊的順序
- **底下還有商品時不允許刪除**，避免商品變成沒有分類的孤兒；
  分類還有子分類時也不允許刪除
- 分類可以有上層分類形成樹狀結構，但**擋掉迴圈** ——
  不能把自己或自己的子孫設為上層

- 一件商品**至少有一個規格**。單一款式的商品也會有一個名為「單一規格」的變體，
  因為購物車與訂單都指向規格而不是商品。
- 價格與庫存掛在**規格**上，不是商品上。商品上的 `basePrice` 是顯示用的快取欄位，
  等於所有啟用規格中的最低價；後台改規格價格時會自動同步。
- 商品狀態：`DRAFT`（草稿，前台看不到）、`ACTIVE`（上架）、`ARCHIVED`（封存）。
  前台一律只顯示 `ACTIVE`。
- 商品網址（slug）**建立後不可修改**，避免既有連結與 SEO 失效。後台的欄位是唯讀的。

**新增商品的流程**（後台）

1. 填名稱、售價、庫存，選品牌與分類，**預設存成草稿**
2. slug 與 SKU 沒填就自動產生；slug 撞名會自動加 `-2`、`-3`
3. 建立後進入編輯頁上傳圖片、加規格
4. 確認無誤再把狀態改成「上架中」

之所以預設草稿：商品在沒有圖片的狀態下被顧客看到，比晚幾分鐘上架糟得多。

**商品圖片**

- 上傳後**一律轉成 WebP，並把最長邊縮到 1600px**。手機直拍常是 4000px／5MB，
  原檔上架會讓商品頁重到不能用（實測 2400×1800 的 PNG 從 114 KB 降到 8.7 KB）。
- 檔名一律重新產生（UUID），不使用上傳者提供的檔名。
- **格式由實際解碼結果判斷**，不看副檔名與 MIME —— 把 `.sh` 改名成 `.png` 騙不過。
- 依 EXIF 自動轉正，手機直拍的照片不會躺著。
- **第一張是主圖**，用在商品列表與社群分享預覽，可在後台調整順序。

**刪除的規則**（商品與規格共用同一個原則）

| 情況 | 行為 |
|---|---|
| 已經賣過 | **不刪除，改為封存／停用**。歷史訂單、發票與評論的關聯要留著 |
| 從未賣過 | 允許永久刪除，磁碟上的圖片資料夾一併移除 |
| 仍有未付款訂單佔用庫存 | 拒絕，要等訂單結束（付款或逾期取消）才能刪 |
| 商品只剩一個規格 | 拒絕刪除該規格 —— 商品至少要有一個規格才能被購買 |

後台按下刪除前會先說明「這個商品已有 N 筆銷售紀錄，將改為封存」，
不會讓營運誤以為資料真的消失了。

**列表頁行為**：可依品牌、價格區間、關鍵字篩選，並依最新／價格／名稱排序。
改動任何篩選條件都會**回到第 1 頁** —— 否則使用者會停在一個超出範圍的空白分頁上。

**搜尋**用子字串比對（`contains`），比對範圍是商品名稱、摘要、品牌名稱。
沒有用全文檢索，因為中文沒有詞邊界，全文索引在這個規模下沒有實質好處。

### 3.2 會員與登入

**三種登入方式共用同一個會員帳號**，可以並存也可以互相綁定：

| 方式 | 帳號識別 | 憑證 |
|---|---|---|
| Google | Email | Google OAuth |
| Email 密碼 | Email | argon2id 雜湊 |
| 手機驗證碼 | 手機號碼 | 6 位數簡訊驗證碼 |

**合併規則**

- Google 與 Email 密碼**以 Email 合併**。同一個 Email 用 Google 登入過，
  之後再用該 Email 註冊密碼，會補上密碼到同一個帳號，不會開出第二個會員。
  之所以敢這樣做，是因為 Google 的 Email 一定經過驗證。
- 手機**以號碼合併**，號碼有唯一性約束。
- 用手機驗證碼登入時，**沒有這支號碼就直接建立會員**（等同註冊），預設名稱是「會員」+ 末四碼。
  這降低了首次購買的摩擦。

**解除綁定的護欄**：系統會計算剩餘可用的登入方式，若解除後會變成零，直接拒絕並提示。
使用者不可能把自己鎖在門外。

**手機驗證碼規則**

| 項目 | 值 |
|---|---|
| 碼長 | 6 位數（CSPRNG 產生，非 `Math.random`） |
| 有效時間 | 5 分鐘 |
| 可嘗試次數 | 5 次，超過即作廢 |
| 重新索取間隔 | 60 秒 |
| 每支號碼每小時上限 | 5 次 |
| 舊碼處理 | 索取新碼時舊碼立即作廢，同時只有一組有效 |

驗證碼以雜湊儲存，明碼只出現在簡訊內容裡。
`SMS_PROVIDER=console` 時明碼會額外寫入資料庫供開發與自動測試讀取，**正式供應商不會留明碼**。

### 3.3 購物車

- **訪客也有購物車**，用一組 30 天有效的匿名識別碼（cookie）認人。
- **登入時會把匿名購物車併入會員購物車**，同一個規格的數量**相加**而不是覆蓋。
  這樣「先加購物車、想結帳時才登入」不會掉東西。
- 加入購物車時檢查可售量；要求數量超過可售量會拒絕，並告知目前最多能買幾件。
- 購物車顯示的運費是**用超商運費估算**的，因為此時還不知道使用者要選哪種配送。
  頁面上明寫「運費以結帳時選擇的配送方式為準」。

### 3.4 金額計算與促銷

計算順序（這個順序會影響結果，是刻意決定的）：

```
小計      = Σ(規格售價 × 數量)
折扣      = 依折扣碼類型計算
運費      = 若（小計 − 折扣）≥ 免運門檻 → 0，否則依配送方式收費
應付總額  = max(0, 小計 − 折扣 + 運費)
```

**折扣碼類型**

| 類型 | 計算 | 備註 |
|---|---|---|
| `PERCENT` | `floor(小計 × 百分比 / 100)` | 無條件捨去到元，綠界只收整數 |
| `FIXED` | `min(折抵額, 小計)` | 折抵不會超過商品金額，總額不會變負數 |
| `FREE_SHIPPING` | 運費歸零 | 不折商品金額 |

**一張訂單只能用一組折扣碼。**

**免運門檻是看折扣後的金額**，不是原始小計。例如小計 1600 達標，
但套用折 200 的折扣碼後剩 1400，就要收運費。這個決定的理由是：
門檻應該反映消費者實際付出的商品金額，否則折扣碼會變成免運的後門。

**折扣碼的限制條件**（任何一項不符就不能用，並回傳具體原因）：
啟用狀態、生效起訖日、全站總使用次數上限、最低消費金額、每人使用次數上限。

**運費是單筆訂單一次計費**，不看件數、重量或材積。這是刻意簡化。

### 3.5 庫存

**兩個欄位**：`stock`（實際在庫）與 `reservedStock`（已被未付款訂單佔住）。
**可售量 = stock − reservedStock**，前台顯示與加入購物車都以可售量為準。

生命週期：

| 時機 | 動作 |
|---|---|
| 訂單成立（尚未付款） | `reservedStock += 數量`（預扣） |
| 付款成功 | `stock -= 數量`、`reservedStock -= 數量`（轉為實扣） |
| 訂單取消或逾期 | `reservedStock -= 數量`（釋放） |

**防超賣的做法**：預扣用一句帶條件的 `UPDATE ... WHERE stock − reservedStock >= 數量` 完成
「檢查 + 佔用」，而不是先查詢再更新。後者在兩個人同時搶最後一件時會雙雙通過檢查而超賣。
條件不成立時影響列數為 0，據此判斷庫存不足並整筆交易回滾。

**逾期釋放**：背景工作每 5 分鐘掃一次，把超過保留時間（預設 30 分鐘）仍未付款的訂單
取消、還原庫存、寄出取消通知。釋放前會在交易內重讀訂單狀態，避免與剛進來的付款通知打對台。

**後台調整庫存的護欄**：不能把庫存調到低於 `reservedStock`，否則會出現負的可售量。

### 3.6 訂單生命週期

```
                    ┌──────────────────┐
                    │ PENDING_PAYMENT  │ 訂單成立，庫存已預扣
                    └────────┬─────────┘
              逾期/人工取消   │   綠界付款成功通知
              ┌──────────────┴──────────────┐
              ▼                             ▼
       ┌────────────┐                  ┌────────┐
       │ CANCELLED  │                  │  PAID  │ 庫存轉實扣
       └────────────┘                  └───┬────┘
                                           │ 物流單建立成功
                                           ▼
                                     ┌────────────┐
                                     │ PROCESSING │ 備貨中
                                     └─────┬──────┘
                                           │ 物流回拋：運送中／已到店
                                           ▼
                                      ┌─────────┐
                                      │ SHIPPED │
                                      └────┬────┘
                                           │ 物流回拋：已取貨
                                           ▼
                                     ┌───────────┐      ┌──────────┐
                                     │ COMPLETED │ ───▶ │ REFUNDED │（人工）
                                     └───────────┘      └──────────┘
```

**誰能改變狀態**

| 狀態 | 由誰設定 |
|---|---|
| `PENDING_PAYMENT` | 訂單成立時 |
| `PAID` | **只有綠界的付款結果背景通知**（`ReturnURL`） |
| `PROCESSING` | 物流單建立成功後自動 |
| `SHIPPED` / `COMPLETED` | 物流狀態回拋自動；管理員也可手動 |
| `CANCELLED` | 逾期排程，或管理員（**僅限尚未付款的訂單**） |
| `REFUNDED` | 只能管理員手動 |

管理員能手動指定的狀態只有 `PROCESSING`、`SHIPPED`、`COMPLETED`、`REFUNDED`。
`PENDING_PAYMENT`、`PAID`、`CANCELLED` 不開放手動設定 —— 這三個牽涉金流與庫存的真實狀態，
不該用改狀態的方式繞過。

**已完成的訂單不會被較早的物流狀態往回推**（`COMPLETED` 不會被回拋改成 `SHIPPED`），
因為綠界的回拋順序不保證。

**訂單內容是快照**。下單當下就把商品名稱、規格、單價、圖片複製進訂單項目，
之後商品改名、改價、下架都不影響歷史訂單與發票。

### 3.7 金流

採用綠界**全方位金流（AIO）**，支援三種付款方式：

| 方式 | 流程 |
|---|---|
| 信用卡 | 導向綠界收銀台刷卡，即時得知結果 |
| ATM 虛擬帳號 | 取號後轉帳，繳費期限以天計 |
| 超商代碼繳費 | 取號後至超商繳費，繳費期限以分鐘計 |

綠界的付款期限與**庫存保留時間對齊**，避免出現「消費者繳費成功但庫存已被釋放」。

#### 安全規則（重要）

1. **`ReturnURL`（背景通知）是判斷付款成功的唯一權威來源。**
   前台導回的 `OrderResultURL` 是使用者的瀏覽器送來的，可以偽造，只用於顯示結果頁面。
2. **每一筆回拋都要驗簽**（CheckMacValue）。驗簽失敗不處理、不重試，記錄下來供人工檢視。
3. **金額必須與訂單一致**。綠界回報的金額與我們記錄的不符時，付款標記失敗並中止，
   不會出貨。這道檢查防的是竄改金額低價成交。
4. **正式環境拒絕模擬付款**。測試環境的模擬付款也會回成功碼，
   但帶有 `SimulatePaid=1`；正式環境收到就當作未付款並記錄異常。
5. **冪等**。所有回拋先落地成事件紀錄，以（來源、類型、內容指紋）唯一，
   綠界重送同一筆不會重複扣庫存。付款處理本身另外再檢查一次訂單狀態，雙重保險。

#### 回拋失敗怎麼辦

處理過程出錯時回傳失敗碼讓綠界重送，同時把錯誤訊息寫進事件紀錄。
管理員可在**後台 → Webhook** 看到所有未處理的事件與失敗原因，並手動重送。
簽章無效的事件**不允許重送**，因為內容不可信。

### 3.8 物流

| 類型 | 通路 |
|---|---|
| 超商取貨（C2C） | 7-ELEVEN、全家、萊爾富、OK |
| 宅配 | 黑貓宅急便、中華郵政 |

- 超商取貨與宅配在綠界是**兩個不同的商店代號**，需分別申請，設定也分開。
- 超商取貨的門市由消費者在**綠界電子地圖**上選擇，選完後門市代號、名稱、地址帶回結帳頁。
- **一張訂單只有一張物流單**，不支援分批出貨。
- 物流訂單編號是「訂單編號 + L」，因為綠界要求金流與物流的交易編號不能相同。
- 商品名稱送給綠界前會清掉綠界不接受的符號，並依通路截斷長度（C2C 25 字、宅配 50 字）。
  踩到限制會被綠界直接退件。

**物流狀態對照**：綠界每個通路有自己的狀態碼表。系統只對應「對消費者有意義」的里程碑
（已建單、運送中、已到店、已取貨、已退回、失敗），**認不得的代碼不會亂改狀態**，
只記錄原始代碼與訊息。這比猜錯安全。

> ⚠️ 目前的對照表只收錄常見代碼，上線前應對照綠界文件的完整狀態表補齊。

### 3.9 電子發票

付款成功後自動開立（可用 `ECPAY_INVOICE_AUTO_ISSUE` 關閉改為人工開立）。

**四種開立方式**，結帳時由消費者選擇：

| 方式 | 條件 |
|---|---|
| 會員載具 | 需登入 |
| 手機條碼載具 | 格式為斜線加 7 碼，例如 `/ABC1234` |
| 捐贈 | 愛心碼 3–7 位數字 |
| 公司統編 | 統編 8 位數字 + 公司抬頭 |

**規則**

- 有統編時**強制列印紙本**，且不能同時使用載具或捐贈。
- **發票明細必須包含運費與折扣**（折扣以負數品項呈現），
  否則品項加總與發票總額對不起來，會被綠界退件。
- **一張訂單只開一次發票**。已開立的不會重複開。
- 退款時需由管理員**手動作廢**發票，並填寫作廢原因（綠界限制 20 字內）。

### 3.10 商品評論

- **只有買過的人能評論**：必須有一筆屬於本人、且訂單狀態為「已完成」的購買項目。
- **每個購買項目只能評論一次**（資料庫層唯一約束）。
- 評論送出後狀態為「待審核」，**經管理員核可才會顯示在商品頁**。
- 商品頁上的評論者名稱會遮罩（只顯示姓氏 + 星號），不公開會員全名。

這套設計讓灌水評論在結構上就做不到 —— 沒有已完成的訂單就沒有可評論的對象。

### 3.11 通知信

| 時機 | 內容 |
|---|---|
| 付款成功 | 訂單明細、金額、配送方式與取貨門市／地址 |
| ATM／超商取號完成 | 虛擬帳號或繳費代碼、金額、繳費期限 |
| 已出貨 | 物流方式、貨態單號、取貨門市 |
| 訂單逾期取消 | 告知庫存已釋放，可重新下單 |

信件內容在**寄送當下即時從資料庫組出來**，所以重試時寄到的一定是最新狀態。
寄信失敗會以指數退避重試最多 5 次。

### 3.12 訪客訂單查詢

用**訂單編號 + 下單時填的手機或 Email** 雙條件比對。

只憑訂單編號不能查詢 —— 否則任何人拿到出貨單就能看到別人的收件資訊。

### 3.13 站內客服聊天

網站右下角常駐聊天按鈕，**未登入也能發問** —— 購買前的問題大多發生在還沒有帳號的時候。

- **訪客身分**：登入者綁會員帳號；未登入者用 `sagon_chat` cookie（保留 90 天）。
  這組 id 刻意與購物車的 `sagon_cart` 分開 —— 購物車的 id 在登入合併時會換發，
  換掉之後訪客就找不回自己的客服對話了。
- **登入後自動接續**：同一台瀏覽器未登入時問過的對話，登入後會歸入該會員名下，兩邊都查得到。
- **每位訪客同時只有一串未結案對話**；客人在結案後再發言會自動重新開啟，不會另開一串。
- **對話 id 一律由伺服器依 cookie／session 決定**，前端不能指定 —— 否則任何人都能用別人的
  id 偷看對話內容。
- **節流**：同一串對話每分鐘最多 20 則訪客訊息，單則上限 2000 字。
- **即時性**：以 SSE 推送，Redis 只負責發「有新訊息」的喚醒訊號，內容一律回資料庫查。
  Redis 掛掉時即時性退化成約 15 秒的定時補查，聊天不會中斷。

| 狀態 | 意思 |
|---|---|
| 待回覆 | 客人發言後還沒有人回 |
| 已回覆 | 客服回過，等客人 |
| 已結案 | 客服手動結案；客人再發言會自動轉回待回覆 |

客服端與客人端各自記未讀數，打開對話即清零。後台側邊欄的紅點是**每次導覽時重算**，
不做即時推播 —— 差幾秒無所謂，不值得為它多開一條長連線。

### 3.14 後台作業

| 區塊 | 能做什麼 |
|---|---|
| 總覽 | 今日營收與訂單數、待付款、待出貨、低庫存、未處理 Webhook |
| 訂單 | 依狀態與關鍵字查詢、看完整明細與物流軌跡、建立物流單、列印單據、開立／作廢發票、改狀態、取消 |
| 商品 | **新增**、基本資料與 SEO、上下架、**圖片上傳／排序／刪除**、**規格增刪**、價格與庫存、分類歸屬、封存／刪除 |
| 分類與品牌 | 新增、編輯、排序、刪除（有商品時受保護） |
| 優惠券 | 新增、啟用／停用、看使用次數 |
| 評論 | 審核通過／退回（可記錄退回原因） |
| 客服訊息 | 依狀態篩選、即時看訪客發言、回覆、標記結案／重新開啟 |
| 會員 | 查詢、看登入方式、有效訂單數與累積消費 |
| Webhook | 檢視所有綠界回拋原始內容、失敗原因、手動重送 |

**所有會改到訂單、商品、優惠券的後台操作都會寫稽核紀錄**（誰、何時、改了什麼、來源 IP），
事後對帳或客訴時查得出來。

---

## 4. 關鍵商業決策與理由

| 決策 | 為什麼 |
|---|---|
| 訪客可以不註冊就結帳 | 註冊是購買轉換率最大的摩擦點 |
| 手機驗證碼登入等同註冊 | 台灣消費者習慣，且免去記密碼 |
| Google 與 Email 以 Email 自動合併 | 避免同一個人有兩個會員、訂單歷史分散 |
| 免運門檻看折扣後金額 | 不讓折扣碼變成免運的後門 |
| 運費單筆計費，不看重量材積 | 睡衣類商品重量差異小，簡化遠比精算划算 |
| 庫存預扣 30 分鐘 | 足夠完成 ATM 轉帳的臨櫃操作，又不會讓熱門商品被長期佔住 |
| 訂單內容存快照 | 商品改價改名不能影響已成立的訂單與發票 |
| 付款狀態只信背景通知 | 前台導回可偽造，這是最基本的金流防線 |
| 認不得的物流代碼不改狀態 | 猜錯會讓消費者收到錯誤的出貨通知 |
| 只有買過的人能評論 | 從結構上排除灌水，不必依賴事後偵測 |
| 評論需審核才公開 | 選品店的品牌調性禁不起未過濾的負評與廣告 |
| 後台不能手動設為「已付款」 | 金流狀態不該能用改欄位繞過 |
| 後台沒有「升為管理員」介面 | 權限提升不該是點兩下的事 |
| slug 不可修改 | 保護既有連結與 SEO |
| 新增商品預設為草稿 | 沒有圖片的商品被顧客看到，比晚幾分鐘上架糟得多 |
| 圖片一律轉 WebP 並縮到 1600px | 選品店靠圖片賣東西，但手機直拍的原檔會讓頁面重到不能用 |
| 圖片格式以實際解碼判斷 | 副檔名與 MIME 都是使用者說的，不能信 |
| 賣過的商品／規格只能封存停用 | 歷史訂單、發票、評論的關聯不能斷，對帳與客訴要查得回來 |
| 分類／品牌有商品時不能刪 | 避免商品變成沒有歸屬的孤兒 |
| 導覽列分類不做快取 | 一個走索引、回 12 列的查詢；加了快取反而讓後台改完要等幾分鐘才看到 |
| 前台逐次請求渲染而非靜態頁 | 內容永遠正確；這個規模下靜態化的效益不值得換來「第一批訪客看到殘缺導覽列」的風險（詳見 README） |

---

## 5. 已知缺口（需要商業決策）

這些是**目前的實際行為**，不是 bug，但都需要你決定要不要改：

### 5.1 訂單取消後不會歸還優惠券使用次數

優惠券的使用次數在**訂單成立時**就計入，訂單逾期取消或人工取消時**不會回復**。

**影響**：消費者套用「每人限用一次」的折扣碼、下單後沒付款，這張折扣碼就用掉了，
之後無法再用。全站總量上限也一樣會被棄單消耗。

**選項**：(a) 維持現狀，客服個案處理；(b) 取消時回復次數並刪除兌換紀錄；
(c) 改成付款成功才計入使用次數（但要處理併發搶最後一個額度的問題）。

### 5.2 每人使用次數上限只對登入會員生效

未登入結帳沒有身分可以綁定，所以「每人限用 N 次」對訪客等於無限制。

**選項**：(a) 維持現狀；(b) 限制特定折扣碼必須登入才能使用；
(c) 以 Email 或手機號碼做次數統計（但可以換 Email 繞過）。

### 5.3 「已退款」只是記帳，沒有真的退刷

把訂單標記為 `REFUNDED` **不會呼叫綠界的退刷 API**，實際退款要到綠界後台手動操作。
綠界的退刷端點已經在設定裡定義好，但沒有任何程式使用它。

**選項**：(a) 維持現狀，寫進客服 SOP；(b) 實作退刷 API 串接（含部分退款）。

### 5.4 其他

| 缺口 | 說明 |
|---|---|
| 不支援分批出貨 | 一張訂單只有一張物流單 |
| 不支援部分退款 | 只有整筆的狀態標記 |
| 圖片存在本機磁碟 | 正式環境已掛 docker volume，重建容器不掉圖；但**沒有備援與 CDN**，單機故障就取不到圖。要換 S3／R2 只需改 `src/lib/uploads.ts` 這一個模組 |
| 商品描述是原始 HTML，未做 sanitize | 只有管理員能編輯，風險有限；但若日後開放給非管理員角色，必須先加白名單過濾 |
| 沒有商品庫存異動歷史 | 只有稽核紀錄，沒有專門的庫存帳 |
| 沒有會員分級或紅利點數 | |
| 電子發票尚未完成實測 | 需先在綠界後台設定發票字軌，詳見 README |

---

## 6. 邊界情況與處理方式

| 情況 | 系統行為 |
|---|---|
| 兩人同時搶最後一件 | 只有一人成單，另一人收到「庫存不足，最多可購買 N 件」 |
| 綠界重送同一筆付款通知 | 第二次直接確認，不重複扣庫存 |
| 綠界回報金額與訂單不符 | 付款標記失敗、不出貨、記錄異常供人工處理 |
| 物流狀態回拋比建單回應更早到 | 用訂單編號回推找到物流單，並補上物流編號 |
| 購物車裡的商品在結帳前下架 | 結帳時拒絕並指出是哪一件，要求先移除 |
| 折扣碼在結帳瞬間被別人用完 | 訂單不成立，提示移除折扣碼後重試 |
| 使用者付款後直接關掉瀏覽器 | 背景通知照樣進來，訂單正常轉為已付款並寄信 |
| 結果頁比背景通知先到 | 結果頁輪詢訂單狀態約 30 秒，狀態一變就自動更新畫面 |
| 資料庫短暫斷線導致回拋處理失敗 | 事件已落地，可在後台手動重送 |
| 寄信服務暫時掛掉 | 指數退避重試 5 次，失敗留在佇列供檢視 |
| 使用者想解除唯一的登入方式 | 拒絕，並提示先設定其他登入方式 |
| 一次上傳 10 張，其中一張損毀 | 其餘 9 張照樣存好，並逐一告知哪幾張失敗、原因是什麼 |
| 上傳把 `.sh` 改名成 `.png` 的檔案 | 解碼失敗，拒絕並回報「圖片無法解析」 |
| 想刪除賣過的商品 | 自動改為封存，並在確認視窗先說明會這樣處理 |
| 想把分類設為自己的子分類 | 拒絕，避免形成永遠展不開的迴圈 |

---

## 7. 上線前的商業確認清單

技術面的檢查清單在 [README.md](README.md)，這裡只列需要**商業決策**的：

- [ ] 運費與免運門檻是否符合實際物流成本
- [ ] 庫存保留 30 分鐘對 ATM 付款是否足夠
- [ ] 第 5 節的三個缺口各自要採哪個選項
- [ ] 退換貨政策的實際條文（目前 FAQ 寫的是 7 天鑑賞期、貼身衣物拆封不退，需法務確認）
- [ ] 發票是否要自動開立，或改為出貨時才開
- [ ] 評論審核由誰負責、多久處理一次
- [ ] 客服信箱與客服時間（目前是 `service@sagon.local`、週一至週五 10:00–18:00）
- [ ] 商品文案與圖片換成自有素材（現有素材為他人著作權，見 README 開頭）
- [ ] 圖片是否要改放物件儲存（S3／R2）而非本機磁碟
- [ ] 商品圖片的尺寸與構圖規範（目前只限制最長邊 1600px，沒有規定長寬比）
