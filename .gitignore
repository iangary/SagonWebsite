node_modules/
.next/
out/
dist/
.turbo/

# env（.env.production 含正式金鑰，絕對不能進版控）
.env
.env.local
.env.production
.env.*.local

# 主機端設定檔的本機副本。裡面是從 /srv/sagon 抓下來的實體檔案 ——
# 資料庫密碼、AUTH_SECRET、綠界與黑貓金鑰都在裡面，一律不進版控。
docs/sagon-deploy/

# 爬蟲產出（他人著作權素材，僅供本機開發）
public/uploads/
prisma/seed-data/

# 黑貓託運單 PDF。上面有收件人的姓名、地址、電話，不進版控也不放 public/。
storage/

# 測試
coverage/
test-results/
playwright-report/
.playwright/

# Claude Code 的本機工具設定。skills 下面是各自獨立的 git repo，
# 提交進來會變成壞掉的 gitlink，clone 的人拿不到內容。
.claude/skills/
# settings.local.json 是個人的權限設定，不共用（settings.json 才是團隊共用的那份）
.claude/settings.local.json

# 編輯器 / OS
.DS_Store
Thumbs.db
*.tsbuildinfo
next-env.d.ts
